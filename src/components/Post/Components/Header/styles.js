import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  name: {
    alignSelf: 'center',
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
});

export default styles;
