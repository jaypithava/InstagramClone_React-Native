import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  name: {
    alignSelf: 'center',
    fontWeight: 'bold',
    color: '#000000',
  },
});

export default styles;
